# Burrows-Wheeler transform

Implements a simple BW class, allowing to compute its BW transform efficiently, as well as providing a function to search patterns in a string and reverse the BW transform easily.
